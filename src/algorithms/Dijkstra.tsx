import { EdgeData, NodeData, Point } from "../types/types";

function dijkstra(srcId: string, nodes: Map<string, NodeData>, edges: Map<string, EdgeData>) {
    // Calculate shortest path to all nodes reachable from srcId
    


};

// minheap of an int




export { dijkstra };